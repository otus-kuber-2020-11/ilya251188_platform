apiVersion: networking.k8s.io/v1beta1
kind: ingress
metadata:
  name: frontend
spec:
  rules:
    host: shop.34.122.143.57.nip.io
    http:
      paths:
        - path: /
          backend:
            serviceName: frontend
            servicePort: 80