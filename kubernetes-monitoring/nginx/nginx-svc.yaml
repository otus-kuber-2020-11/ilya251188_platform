apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
  namespace: nginx
spec:
  type: ClusterIP
  selector:
    app: nginx
  ports:
    - port: 80
      name: http
      targetPort: 80
    - port: 8080
      name: stub
      targetPort: 8080